# Roadmap

Priority-ordered backlog. Commit subjects reference these item numbers.
Completed items are removed here and recorded in docs/CHANGELOG.md.

## P1

1. Start/stop a wf-recorder capture (spawn, track PID, SIGINT to stop).
2. Status panel: elapsed timer + growing file size.
3. Live audio level meter.

## P2

4. Window source (compositor IPC geometry captured as a region).
5. Audio-only mode (flac) for lean transcripts.
6. Whisper handoff (configured transcription command after stop).
7. wl-screenrec backend option (hardware encode).
8. Config file (default output dir, container, default audio source).
