# Roadmap

Priority-ordered backlog. Commit subjects reference these item numbers.
Completed items are removed here and recorded in docs/CHANGELOG.md.

## P1

1. Live audio level meter.

## P2

2. Window source (compositor IPC geometry captured as a region).
3. Audio-only mode (flac) for lean transcripts.
4. Whisper handoff (configured transcription command after stop).
5. wl-screenrec backend option (hardware encode).
6. Config file (default output dir, container, default audio source).
