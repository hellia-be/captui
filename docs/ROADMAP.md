# Roadmap

Priority-ordered backlog. Commit subjects reference these item numbers.
Completed items are removed here and recorded in docs/CHANGELOG.md.

## P1

1. Status panel: elapsed timer + growing file size.
2. Live audio level meter.

## P2

3. Window source (compositor IPC geometry captured as a region).
4. Audio-only mode (flac) for lean transcripts.
5. Whisper handoff (configured transcription command after stop).
6. wl-screenrec backend option (hardware encode).
7. Config file (default output dir, container, default audio source).
